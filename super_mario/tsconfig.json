{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2016" /* Define a versão do JavaScript para a qual o TypeScript será compilado. Pode ser es2016 ou uma versão mais recente. */,

    /* Modules */
    "module": "es6" /* Especifica que os módulos gerados devem seguir o padrão ES6. */,
    "rootDir": "./src" /* Especifica a pasta raiz dos arquivos de origem. */,
    "outDir": "./build" /* Especifica a pasta de saída para todos os arquivos compilados. */,

    /* Emit */
    "declaration": true /* Gera arquivos .d.ts para o TypeScript e JavaScript no projeto. */,
    "declarationMap": true /* Cria mapas de sourcemap para arquivos .d.ts. */,
    "sourceMap": true /* Cria arquivos de mapa de origem (sourcemaps) para os arquivos JavaScript gerados. */,
    "esModuleInterop": true /* Facilita a interoperabilidade com módulos CommonJS ao emitir JavaScript adicional. */,
    "forceConsistentCasingInFileNames": true /* Garante que a diferenciação de maiúsculas e minúsculas seja correta em importações. */,

    /* Type Checking */
    "strict": true /* Ativa todas as opções de verificação de tipos rigorosas. */,
    "skipLibCheck": true /* Ignora a verificação de tipos em arquivos .d.ts. */
  },
  "include": ["./src/**/*"],
  "exclude": ["node_modules", "build"]
}
